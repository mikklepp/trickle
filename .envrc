# AWS Configuration
# Copy this file to .env and fill in your values
# Then source it: source .env

# AWS region for all resources (SES, DynamoDB, Lambda, etc.)
# Default: eu-west-1
export AWS_REGION=eu-west-1

# AWS CLI profile to use for credentials
# If you have multiple AWS profiles configured, set this to use a specific one
# Leave empty to use default profile
export AWS_PROFILE=QED

# Optional: SST stage (dev, staging, production, etc.)
# Default: dev
export CDK_STAGE=mikko
